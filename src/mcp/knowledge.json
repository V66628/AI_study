{
  "rules": [
    "严格遵守 IDL 中每个字段的类型与嵌套结构，不得增删字段",
    "手机号：1 开头的 11 位数字，如 13800138000",
    "邮箱：合法格式，如 zhangsan@example.com",
    "URL/头像：使用 https:// 开头的真实感地址",
    "时间字段：ISO 8601 格式字符串，如 2026-09-24T12:00:00Z",
    "ID：正整数，从 1 开始",
    "状态码 code：成功固定为 0，message 为 success",
    "枚举字段：只能从字段描述给出的枚举值中选取",
    "数组字段：生成 2-3 条元素，元素结构同样遵守 IDL",
    "普通字符串：结合字段语义生成真实感内容（中文名、常见单词），不要使用 test、xxx、123 等占位值",
    "只返回 mock 数据本身的 JSON，不要包含解释、注释或 Markdown 代码块标记"
  ]
}
